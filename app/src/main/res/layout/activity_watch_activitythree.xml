<!-- activity_watch_activitythree.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- EditText para mostrar la URL del MP3 (solo lectura, seleccionable) -->
    <EditText
        android:id="@+id/editTextMp3Url"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="URL del MP3"
        android:inputType="textUri"
        android:enabled="true"
        android:focusable="false"
        android:longClickable="true"
        android:textIsSelectable="true" />

    <!-- Botón para copiar la URL al portapapeles -->
    <Button
        android:id="@+id/copyButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Copiar URL" />

    <!-- WebView para cargar la URL del blob -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="200dp"/>

    <!-- Botón para iniciar la descarga -->
    <Button
        android:id="@+id/downloadButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Descargar" />

</LinearLayout>
